# Backend URL for MP3 conversion (optional)
# If not set, MP3 download will show an error
# Point this to your backend service (see /backend folder)
BACKEND_URL=http://localhost:3001

# Or use public URL if backend is deployed
# BACKEND_URL=https://your-backend-domain.com
